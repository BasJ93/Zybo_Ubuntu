#!/bin/sh
set -e
# Automatically added by dh_installinit
if ([ -x "/etc/init.d/dbus" ] || [ -e "/etc/init/dbus.conf" ]) && \
   [ "$1" = remove ]; then
	invoke-rc.d dbus stop || exit $?
fi
# End automatically added section
