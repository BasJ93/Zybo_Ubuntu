#!/bin/sh
set -e
# Automatically added by dh_installinit
if ([ -x "/etc/init.d/apache2" ] || [ -e "/etc/init/apache2.conf" ]) && \
   [ "$1" = remove ]; then
	invoke-rc.d apache2 stop || true
fi
# End automatically added section
